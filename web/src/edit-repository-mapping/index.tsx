import React from 'react';
import ReactDOM from 'react-dom';
import '../index.css';
import EditRepositoryMappingPage from './component/edit-repository-mapping-page';

ReactDOM.render(
  <React.StrictMode>
    <EditRepositoryMappingPage />
  </React.StrictMode>,
  document.getElementById('root'),
);
