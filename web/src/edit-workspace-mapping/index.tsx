import React from 'react';
import ReactDOM from 'react-dom';
import '../index.css';
import EditWorkspaceMappingPage from './component/edit-workspace-mapping-page';

ReactDOM.render(
  <React.StrictMode>
    <EditWorkspaceMappingPage />
  </React.StrictMode>,
  document.getElementById('root'),
);
